# Headline

A module to validate files in the 101repo

# Details

The "validator"s of 101meta are applied to matched files.

# Dependencies

This module assumes that files have been matched by the module "match101meta".

