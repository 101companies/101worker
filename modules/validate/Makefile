include ../Makefile.vars
output = ${ontoDir}

all:
	make run

run:
	cp -r models ${output}

# Remove local temporary files, if any
clean:
	@#no-op

# Remove files created by this module
reset:
	@#no-op
