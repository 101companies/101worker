{# note: disabled template because the query does not return anything

language_or_technology count

{% for x in data %}
 {{ x['language_or_technology']['value'] }}; {{ x['count']['value'] }}
{% endfor %}

#}