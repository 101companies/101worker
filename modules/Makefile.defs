#
# Important directories
#

# The 101companies:Repository in the 101worker workspace
repoDir = ../../../101results/101repo

# GeSHi in the 101worker workspace
geshiDir = ../../../101results/geshi

# The web space filled by the 101worker modules
webDir = ../../../101web

# The dumps directory filled by the 101worker modules
dumpsDir = ${webDir}/dumps

# The directory for annotating contributions
contrDir = ${webDir}/contributions

# Dumps

rulesDump = ${dumpsDir}/rules.json
matchesDump = ${dumpsDir}/matches.json
geshiDump = ${dumpsDir}/geshi.json
validatorDump = ${dumpsDir}/validator.json
extractorDump = ${dumpsDir}/extractor.json
summaryDump = ${dumpsDir}/summary.json
