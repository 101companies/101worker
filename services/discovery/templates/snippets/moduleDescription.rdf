{% for target in module['targets'] %}
    <co:moduleTarget
        co:scope="{{ target.scope }}"
        co:targetHeadline="{{ target.headline }}"
        co:language="{{ target.language }}"
        {% if target.suffix %}
            co:suffix="{{ target.suffix }}"
        {% endif %}
    />
{% endfor %}